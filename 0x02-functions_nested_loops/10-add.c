#include "main.h"

/**
 * add - function that adds two integers and returns the result
 *
 * @a: input number
 * @b: input number
 *
 * Return: results
*/

int add(int a, int b)
{
	int num = 0;

	num = a + b;
	return (num);
}
